import styled from "styled-components";

export const IconHide = styled.Icon`
  position: absolute;
  right: 10px;
  top: 50%;
  transform: translateY(-12px); /* Ajuste para centralizar o ícone verticalmente */
`;
